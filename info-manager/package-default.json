{
  "name": "info-manager",
  "version": "1.0.0",
  "description": "An information management system built with Electron and SQLite.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . InfoManager --platform=win32 --arch=x64 --out=build --overwrite",
    "init-db": "node ./database/initDB.js"
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "electron": "^26.2.0"
  },
  "dependencies": {
    "sqlite3": "^5.1.6"
  }
}